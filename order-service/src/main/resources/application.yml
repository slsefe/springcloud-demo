server:
  port: 8082
spring:
  application:
    name: order-service
  config:
    import:
      - nacos:order-service.yaml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
